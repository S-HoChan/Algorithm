package ProgrammersLevel3;

import java.util.ArrayList;
import java.util.List;

public class MuziLive {
    public int solution(int[] food_times, long k) {
//        List<Integer> list = new ArrayList<>();
//        for (int foodTime : food_times) list.add(foodTime);
//
//        int cycle = 0;
//        while (k > 0) {
//            int n = list.size();
//            int min = 100000001;
//            int idx = -1;
//
//            for (int i = 0; i < n; i++) {
//                int now = list.get(i) - cycle;
//                if (now < min) {
//                    min = now;
//                    idx = i;
//                }
//            }
//
//            if (min * n <= k) {   // 다 먹는 음식이 생기면
//                k -= n * min;
//                list.remove(idx);
//                cycle = min;
//            } else break;
//        }
//
//        return (int)k + 1;
        return 0;
    }
}
